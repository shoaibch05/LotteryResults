name: Keep Backend Awake

on:
  schedule:
    - cron: '*/10 * * * *'   # every 10 minutes
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Railway backend
        run: curl -fsS https://nodejs-production-40ae.up.railway.app/ping || true
